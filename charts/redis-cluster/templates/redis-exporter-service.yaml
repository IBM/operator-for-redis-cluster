{{- if .Values.metrics.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "icm-redis-cluster.fullname" . }}-exporter
  labels: {{- include "icm-redis-cluster.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.exporter.port.number }}
      targetPort: {{ .Values.metrics.exporter.port.name }}
      protocol: TCP
      name: {{ .Values.metrics.exporter.port.name }}
  selector:
    {{- include "icm-redis-cluster.selectorLabels" . | nindent 4 }}
{{- end }}
